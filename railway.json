{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py makemigrations && python manage.py migrate && python manage.py runserver && celery -A api_root worker -l info && celery -A api_root beat -l INFO --scheduler django_celery_beat.schedulers:DatabaseScheduler && gunicorn mysite.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}